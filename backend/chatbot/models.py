"""Models for the chatbot application.

No models yet. Future candidates (added only when the first endpoints need them):

- Document: metadata of an uploaded file (name, type, status, storage path).
- Conversation / Message: persisted chat history.
- SourceLink: which document passage supported a given answer.

Keep storage decisions here; business logic belongs in services/, not models.
"""
